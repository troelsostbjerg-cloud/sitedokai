---
const steps = [
  {
    num: '01',
    title: 'Send din URL',
    desc: 'Indsend dit website via formularen. Vi begynder med det samme.',
  },
  {
    num: '02',
    title: 'Vi tester dit website',
    desc: 'AI-synlighed på 4 platforme, 1 website og op til 5 nøglesider plus teknisk baseline.',
  },
  {
    num: '03',
    title: 'Få din rapport',
    desc: 'En kort PDF med score, fund og 3 prioriterede forbedringer. Leveret inden for 48 timer på hverdage.',
  },
];
---

<section id="how-it-works" class="bg-white border-b border-gray-200">
  <div class="max-w-5xl mx-auto px-6 py-20 md:py-28">
    <div class="max-w-2xl mb-16">
      <h2 class="font-display text-3xl md:text-4xl font-bold text-brand tracking-tight mb-4">
        Sådan virker det
      </h2>
      <p class="text-lg text-gray-500">
        Fra URL til konkret AI-synlighedstjek. Simpelt og konkret.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-12 md:gap-8">
      {steps.map((step, i) => (
        <div class="relative">
          {i < steps.length - 1 && (
            <div class="hidden md:block absolute top-8 left-full w-full h-px bg-gray-200 -translate-x-4" />
          )}
          <div class="font-display text-5xl font-extrabold text-gray-200 mb-4" aria-hidden="true">{step.num}</div>
          <h3 class="font-display text-lg font-bold text-brand mb-2">{step.title}</h3>
          <p class="text-sm text-gray-500 leading-relaxed">{step.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>
