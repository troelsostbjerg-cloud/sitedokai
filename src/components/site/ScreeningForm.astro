---
import WorkflowScreeningForm from './WorkflowScreeningForm.astro';

interface Props {
  sourcePage?: string;
  title?: string;
  text?: string;
  submitLabel?: string;
  interest?: string;
}

const props = Astro.props as Props;
---

<WorkflowScreeningForm {...props} />
